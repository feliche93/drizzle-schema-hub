{
  "name": "drizzle-schema-hub",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "prettier . --check && pnpm eslint",
    "lint:fix": "prettier . --write && eslint --fix",
    "lint:format": "prettier . --write"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "drizzle-orm": "^0.28.6"
  }
}